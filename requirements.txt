# SylvaGIS API — bağımlılıklar
# server.py taranarak çıkarılmıştır (import satırlarına göre).

flask>=3.0
flask-cors>=4.0
gunicorn>=22.0
requests>=2.31
numpy>=1.26
pyshp>=2.3
earthengine-api>=1.0

# Doğru .prj/CRS dönüşümü için (Faz 3.1 düzeltmesi — önceden .prj dosyası
# her zaman yanlışlıkla WGS84 yazıyordu).
pyproj>=3.6

# ⚠️ KRİTİK: GeoTIFF indirme/mozaikleme/yeniden projeksiyon (rasterio.merge,
# rasterio.warp.reproject, rasterio.mask) VE LULC lejant/renk koruma
# (write_colormap) fonksiyonları rasterio'ya bağımlı — bu paket olmadan
# server.py'daki bu bölümler ImportError ile çöker. requirements.txt'te
# hiç yoktu, buradan eklendi.
rasterio>=1.3

# Karmaşık/bozuk geometrilerin (kendi kendini kesen poligonlar, donut
# birleştirme vb.) onarımı için üçüncü seviye yedek — kod zaten bu paket
# kurulu değilse anlaşılır bir hata veriyordu, ama bazı kullanıcı çizimleri
# için gerçekten gerekli olabilir, bu yüzden baştan kurulu olması önerilir.
shapely>=2.0

# Backend hata izleme (Faz 1.3) — SYLVA_SENTRY_DSN tanımlıysa devreye
# girer, tanımlı değilse kod zaten try/except ile bunu es geçer.
sentry-sdk[flask]>=2.0
